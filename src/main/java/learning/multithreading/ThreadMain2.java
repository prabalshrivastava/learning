package learning.multithreading;

public class ThreadMain2 {
    public static void main(String[] args) {

        //suppose we have a thread is executing but we change the priority of that thread how will it behave

        //to be revisited
        //1. interrupts
        //2. monitor lock
        //3. StackTraceElement
        //4. Thread.State
        //5. Context classloader
        //6. Thread.UncaughtExceptionHandler

//        Thread.sleep();//sleeps the current thread for the specified
//        Thread.currentThread();
//        Thread t1 = new Thread();
//        t1.join();
//        t1.join(10);
//        t1.setPriority(100);
//        Thread[] tarray = new Thread[1000];
//        Thread.enumerate(tarray);
//        Thread

    }
}
